# Node modules
node_modules/

# Build directories
dist/
dist-ssr/
build/

# Vite cache (for faster rebuilds, should not be committed)
.vite/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
.pnp/
.pnp.js

# OS-specific files
.DS_Store          # macOS-specific file
Thumbs.db           # Windows-specific
desktop.ini         # Windows-specific

# Editor directories and files
.vscode/*
!.vscode/extensions.json   # Keep the extensions config if needed
.idea/                   # JetBrains IDEs (IntelliJ, WebStorm, etc.)
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sensitive data (adjust as needed)
.env
.env.local
.env.*.local

# Tailwind and PostCSS config files (keep in version control)
!tailwind.config.js
!postcss.config.js
!index.html

# Testing files
coverage/               # Jest, Mocha, or other test coverage output
*.test.js               # Test files (Jest, Mocha)
*.spec.js               # Test files (Jest, Mocha)

# Temporary files
*.bak                  # Backup files
*.swp                  # Swap files (e.g., Vim)
*.swo                  # Swap files (e.g., Vim)

# Parcel build directory (if used)
.cache/                 # Cache directory generated by some bundlers (e.g., Parcel)

# TypeScript configuration (if needed)
tsconfig.tsbuildinfo    # TypeScript incremental build cache

# Preact-specific files (if applicable)
preact.config.js        # Custom preact config (if any)
